# 빠른 시작 가이드 - RAG 구성 및 사용

## 🚀 3단계로 시작하기

### 1단계: 문서 확인 및 추가 (선택사항)

현재 문서 폴더에 파일이 있습니다:
```
C:\Users\elect\webui\open-webui\backend\data\documents\KCS_10_10_30_2018.pdf
```

**추가 문서가 필요하면:**
- 위 폴더에 PDF, TXT, DOCX 파일을 복사하세요

### 2단계: 문서 임베딩 실행

**방법 A: 배치 파일 실행 (가장 쉬움)**
```bash
# 프로젝트 루트에서
EMBED_DOCUMENTS.bat
```

**방법 B: 수동 실행**
```bash
# Anaconda Prompt에서
conda activate webui
cd C:\Users\elect\webui\open-webui\backend

set DATA_DIR=C:\Users\elect\webui\open-webui\backend\data
set FROM_INIT_PY=true
set VECTOR_DB=chroma

python scripts\embed_documents.py
```

**예상 출력:**
```
Found 1 files in C:\Users\elect\webui\open-webui\backend\data\documents
Loading documents from: ...
Using Vector DB: chroma
Collection: documents
Chunking strategy: lexical
--------------------------------------------------
Embedding Statistics:
  Total documents: 1
  Total chunks: X
  Success: 1
  Failed: 0
--------------------------------------------------
Done! Documents are now embedded in ChromaDB.
```

### 3단계: 웹 UI에서 RAG 설정 확인

1. **브라우저에서 접속**: `http://localhost:8080`
2. **관리자 설정 접속**: 좌측 메뉴 → "설정" → "문서"
3. **설정 확인**:
   - ✅ Vector Database: **ChromaDB** (기본값)
   - ✅ Chunking Strategy: **Lexical** (또는 원하는 전략)
   - ✅ Chunk Size: 1000
   - ✅ Chunk Overlap: 100
   - ✅ Embedding Engine: **sentence-transformers**
   - ✅ Embedding Model: **all-MiniLM-L6-v2**
4. **저장**: "Save" 버튼 클릭

### 4단계: RAG 사용하기

#### 방법 1: 채팅에서 사용
1. 웹 UI에서 새 채팅 시작
2. 질문 입력 (예: "KCS 문서의 주요 내용은?")
3. RAG가 자동으로 관련 문서를 검색하여 답변 생성

#### 방법 2: Knowledge Base 검색
1. 좌측 메뉴 → "Knowledge Base" 또는 "Knowledge"
2. 검색창에 키워드 입력
3. 관련 문서 및 청크 확인

---

## 📋 체크리스트

- [ ] 문서 폴더에 파일 확인 (`data/documents`)
- [ ] `EMBED_DOCUMENTS.bat` 실행 완료
- [ ] 임베딩 성공 메시지 확인
- [ ] 웹 UI에서 RAG 설정 확인
- [ ] 채팅에서 RAG 테스트

---

## 🔧 문제 해결

### 임베딩이 실패하는 경우

1. **Python 환경 확인**
   ```bash
   conda activate webui
   python --version
   ```

2. **필요 패키지 확인**
   ```bash
   pip list | findstr chromadb
   pip list | findstr sentence-transformers
   ```

3. **에러 메시지 확인**
   - 스크립트 실행 시 출력되는 에러 메시지 확인
   - 서버 로그 확인

### 검색 결과가 없는 경우

1. **임베딩 완료 확인**
   - 스크립트가 성공적으로 완료되었는지 확인
   - "Success: 1" 메시지 확인

2. **Collection 이름 확인**
   - 기본 Collection 이름: `documents`
   - 설정에서 다른 이름을 사용한 경우 확인

3. **Vector DB 확인**
   - 설정에서 Vector Database가 ChromaDB로 설정되어 있는지 확인

---

## 📚 더 자세한 정보

- **전체 가이드**: `RAG_사용_가이드.md`
- **아키텍처**: `RAG_ARCHITECTURE.md` (복구 필요)
- **임베딩 상세**: `QUICK_START_EMBEDDING.md` (복구 필요)

---

**지금 바로 시작하세요!**

1. `EMBED_DOCUMENTS.bat` 실행
2. 웹 UI에서 설정 확인
3. 채팅에서 테스트

